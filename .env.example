# RS-Agent 环境变量示例
# 复制为 .env 并填写实际值：cp .env.example .env

# === LLM（Qwen / DashScope）配置 ===
# API Key：至少设置其一
DASHSCOPE_API_KEY=sk-xxx
# LLM_API_KEY=sk-xxx
# OPENAI_API_KEY=sk-xxx

# OpenAI 兼容模式基址（默认阿里云 DashScope）
RS_AGENT_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 模型名：DashScope 有效值为 qwen-turbo / qwen-plus / qwen-max 等
RS_AGENT_LLM_MODEL=qwen-plus

# === KB_QUERY 提质（方案 B）配置 ===
# 是否启用：LLM 多 query 检索增强 + 综合输出（默认 true）
# RS_AGENT_KB_QUERY_LLM_ENABLED=true
# 最多生成多少条检索子问题（默认 4）
# RS_AGENT_KB_QUERY_MAX_SUBQUERIES=4
# 合并后的 KB 文本最多传给 LLM 的字符数（默认 12000，超出会截断）
# RS_AGENT_KB_QUERY_MAX_MERGED_CHARS=12000

# === 文生图（流程图）配置：DashScope 万相，用于「三、系统改动点-后端」流程图 PNG ===
# 是否启用（默认 true）
# RS_AGENT_IMAGE_GEN_ENABLED=true
# 万相文生图接口（默认北京地域同步接口）
# RS_AGENT_IMAGE_GEN_URL=https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation
# 模型名（默认 wan2.6-t2i）
# RS_AGENT_IMAGE_GEN_MODEL=wan2.6-t2i
